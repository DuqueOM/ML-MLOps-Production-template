apiVersion: v1
kind: Service
metadata:
  name: "{service-name}-service"
  labels:
    app: "{service-name}"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: "{service-name}"
