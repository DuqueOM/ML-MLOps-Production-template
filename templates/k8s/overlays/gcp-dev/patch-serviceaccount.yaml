# =============================================================================
# GCP Dev — ServiceAccount with Workload Identity
# =============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{service-name}-sa"
  annotations:
    iam.gke.io/gcp-service-account: "{service-name}-sa-dev@{PROJECT_ID}.iam.gserviceaccount.com"
